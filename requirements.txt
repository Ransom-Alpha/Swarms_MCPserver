# ================================
# 📦 Swarms MCP Server Requirements
# ================================

# --- Core Environment ---
python-dotenv>=1.0.1            # Load environment variables (.env)
fastmcp>=2.2.3                  # Lightweight MCP server framework

# --- File Monitoring ---
watchdog>=4.0.0                 # Watch corpus directory for changes

# --- LangChain Core and Ecosystem ---
langchain==0.3.24               # Core LangChain framework
langchain-core==0.3.56          # Underlying utilities
langchain-community>=0.0.43     # Community document loaders
langchain-openai==0.3.14        # OpenAI integration
langchain-text-splitters==0.3.8 # Chunk splitting logic
rank_bm25>=0.2.2                # BM25 Retriever support for LangChain

# --- Embeddings and Storage ---
langchain-chroma>=0.2.0         # Updated Chroma vector store integration
chromadb==1.0.7                 # Local vector database
tiktoken==0.9.0                 # Token counting for embedding models
SQLAlchemy>=1.4,<3.0.0          # Backend dependency for Chroma

# --- File Parsing ---
unstructured==0.12.3            # Intelligent document parsing (HTML, Markdown, etc.)
python-magic-bin==0.4.14        # File type detection (needed by Unstructured)
pyyaml>=6.0                     # YAML parser support

# --- Networking & API Communication ---
openai-agents-mcp>=0.0.8        # MCP protocol & agent integration (for MCPServer)
httpx>=0.28.1                   # Async HTTP client
openapi-pydantic>=0.5.1         # OpenAPI schema models
exceptiongroup>=1.2.2           # Python 3.11 compatibility fix

# --- Console Enhancements ---
rich>=13.9.4                    # Beautiful CLI logging and formatting
typer>=0.15.2                   # CLI app framework (FastMCP dependency)

# --- OpenAI API ---
openai>=1.68.2,<2.0.0           # OpenAI Python SDK for embeddings


# --- Flow Diagram ---
graphviz

